version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    restart: unless-stopped
    environment:
      - PORT=5000
      - NODE_ENV=production
      - smtp_email=pulkits117@gmail.com
      - smtp_password="dtms jubi ijmf khpa"
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    restart: unless-stopped
    environment:
      - PORT=5173
    ports:
      - "80:5173"
    depends_on:
      - backend
